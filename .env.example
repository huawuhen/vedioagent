PORT=3000

# true = safe demo mode with no paid model calls.
# false + DEEPSEEK_API_KEY = production text generation through the backend proxy.
MOCK_MODE=true

LLM_PROVIDER=deepseek
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_API_KEY=

LLM_MAX_TOKENS=16384
LLM_TIMEOUT_MS=180000

JSON_LIMIT=25mb
